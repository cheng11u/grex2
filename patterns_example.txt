scope: X-[subj]->Y
conclusion: X << Y
conclusion_meta:
    sent_id: "n01.*"

templates:
    base:
        own:
            method: exclude
            pattern: "lemma"
        parent:
            method: include
            pattern: ".*"
        child:
            method: include
            pattern: ".*"
        prev:
            method: include
            pattern: ".*"
        next:
            method: include
            pattern: ".*"

features:
    X: base
    Y: 
        own:
            method: include
            pattern: ".*"
        parent:
            method: include
            pattern: ".*"
        child:
            method: include
            pattern: ".*"
        prev:
            method: include
            pattern: ".*"
        next:
            method: include
            pattern: ".*"
